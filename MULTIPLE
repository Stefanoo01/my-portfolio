// app/layout.tsx
import React from 'react';
import './globals.css';

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body className="min-h-screen antialiased bg-background text-foreground [--radius:10px] bg-[radial-gradient(80%_60%_at_50%_-20%,hsl(var(--muted))/0.35_0%,transparent_60%),radial-gradient(80%_60%_at_50%_120%,hsl(var(--muted))/0.35_0%,transparent_60%)]">
        {children}
      </body>
    </html>
  );
}

// components/site-header.tsx
import React from 'react';
import { Button, Link } from '@nextui-org/react';

export default function SiteHeader() {
  return (
    <header className="sticky top-0 z-40 w-full border-b bg-background/70 backdrop-blur supports-[backdrop-filter]:bg-background/60">
      <div className="relative container mx-auto max-w-6xl px-4 h-16 flex items-center justify-between after:pointer-events-none after:absolute after:inset-x-0 after:-bottom-px after:h-px after:bg-gradient-to-r after:from-transparent after:via-primary/40 after:to-transparent">
        {/* Site logo and navigation links */}
        <nav className="flex items-center space-x-4">
          {['Home', 'About', 'Projects', 'Contact'].map((link) => (
            <Link key={link} href={`/${link.toLowerCase()}`} className="text-muted-foreground hover:text-foreground transition-colors underline-offset-4 hover:underline decoration-primary/40">
              {link}
            </Link>
          ))}
        </nav>
        {/* Mobile menu button */}
        <Button variant="outline" size="icon" className="transition-colors hover:bg-accent bg-transparent">
          Menu
        </Button>
      </div>
    </header>
  );
}

// components/hero.tsx
import React from 'react';
import { Button } from '@nextui-org/react';

export default function Hero() {
  return (
    <section aria-labelledby="hero-heading" className="relative overflow-hidden border-b animate-in fade-in-20 duration-700">
      <div aria-hidden="true" className="pointer-events-none absolute -z-10 inset-0">
        <div className="absolute left-1/2 top-10 -translate-x-1/2 h-64 w-64 rounded-full blur-3xl opacity-40 bg-gradient-to-tr from-primary/25 via-primary/5 to-transparent" />
      </div>
      <div className="container mx-auto max-w-6xl px-4 flex flex-col md:flex-row items-center justify-between">
        <div className="order-2 md:order-1 animate-in slide-in-from-left-8 fade-in-10 duration-700">
          <h1 id="hero-heading" className="text-4xl md:text-6xl font-bold mb-4">
            Welcome to My Portfolio
          </h1>
          <p className="text-lg md:text-xl text-muted-foreground mb-6">
            I am a software developer with a passion for creating innovative solutions.
          </p>
          <div className="flex space-x-4">
            <Button asChild className="shadow-sm hover:shadow-md transition-shadow bg-gradient-to-r from-primary to-primary/80 text-primary-foreground">
              <a href="#projects">View Projects</a>
            </Button>
            <Button asChild variant="outline" className="hover:bg-accent/40 transition-colors bg-transparent">
              <a href="#contact">Get in Touch</a>
            </Button>
          </div>
        </div>
        <div className="order-1 md:order-2 flex justify-center animate-in slide-in-from-right-8 fade-in-10 duration-700">
          <div className="relative h-48 w-48 md:h-64 md:w-64 rounded-full ring-2 ring-primary/20 shadow-xl overflow-hidden transition-transform duration-500 will-change-transform hover:scale-[1.03]">
            {/* Avatar image */}
          </div>
        </div>
      </div>
    </section>
  );
}

// components/projects.tsx
import React from 'react';
import { Card, Button } from '@nextui-org/react';

export default function Projects() {
  const projects = [
    { slug: 'project1', title: 'Project 1' },
    { slug: 'project2', title: 'Project 2' },
    { slug: 'project3', title: 'Project 3' },
  ];

  return (
    <div className="container mx-auto max-w-6xl px-4 animate-in fade-in-20 duration-700">
      <h2 className="text-3xl font-bold mb-6">My Projects</h2>
      <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
        {projects.map((p) => (
          <Card key={p.slug} className="group overflow-hidden border bg-card/60 backdrop-blur-sm transition-all duration-300 hover:shadow-lg hover:-translate-y-0.5">
            <div className="relative aspect-[16/10] overflow-hidden bg-[linear-gradient(to_bottom,transparent,rgba(0,0,0,0.02))]">
              <div className="absolute inset-0">
                <img src={`/${p.slug}.jpg`} alt={p.title} className="object-cover transition-transform duration-500 ease-out group-hover:scale-[1.03]" />
              </div>
            </div>
            <div className="p-4">
              <h3 className="text-xl font-bold mb-2">{p.title}</h3>
              <p className="text-sm text-muted-foreground">Description of the project.</p>
            </div>
            <div className="flex items-center justify-between border-t px-6 py-4">
              <Button asChild className="shadow-sm hover:shadow-md transition-shadow bg-gradient-to-r from-primary to-primary/80 text-primary-foreground">
                <a href={`/${p.slug}`}>View Project</a>
              </Button>
              <Button asChild variant="outline" className="hover:bg-accent/40 transition-colors bg-transparent">
                <a href={`/${p.slug}`}>Code</a>
              </Button>
            </div>
          </Card>
        ))}
      </div>
    </div>
  );
}

// components/experience.tsx
import React from 'react';

export default function Experience() {
  const experienceItems = [
    { year: '2020', title: 'Software Developer', company: 'Company A' },
    { year: '2021', title: 'Senior Developer', company: 'Company B' },
    { year: '2022', title: 'Team Lead', company: 'Company C' },
  ];

  return (
    <div className="container mx-auto max-w-6xl px-4 animate-in fade-in-20 duration-700">
      <h2 className="text-3xl font-bold mb-6">Experience</h2>
      <ol className="relative border-l pl-6 border-muted-foreground/20">
        {experienceItems.map((item) => (
          <li key={item.year} className="mb-6 ml-6">
            <div className="absolute -left-[7px] mt-1 h-3 w-3 rounded-full bg-primary shadow-[0_0_0_3px_hsl(var(--background))] motion-safe:animate-pulse" aria-hidden="true" />
            <h3 className="font-semibold text-lg mb-2">{item.title}</h3>
            <p className="text-muted-foreground mb-4">{item.company}</p>
            <p className="text-sm text-muted-foreground">{item.year}</p>
          </li>
        ))}
      </ol>
    </div>
  );
}

// components/skills.tsx
import React from 'react';
import { Card } from '@nextui-org/react';

export default function Skills() {
  const skillsCategories = [
    { name: 'Frontend', skills: ['React', 'Next.js', 'JavaScript'] },
    { name: 'Backend', skills: ['Node.js', 'Express', 'Python'] },
    { name: 'Database', skills: ['MongoDB', 'PostgreSQL', 'MySQL'] },
  ];

  return (
    <div className="container mx-auto max-w-6xl px-4 animate-in fade-in-20 duration-700">
      <h2 className="text-3xl font-bold mb-6">Skills</h2>
      <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
        {skillsCategories.map((cat) => (
          <Card key={cat.name} className="h-full bg-card/60 backdrop-blur-sm border transition-all duration-300 hover:-translate-y-0.5 hover:shadow-md animate-in fade-in-10 slide-in-from-bottom-4">
            <div className="p-4">
              <h3 className="text-xl font-bold mb-2">{cat.name}</h3>
              <ul className="list-disc list-inside text-sm text-muted-foreground">
                {cat.skills.map((skill) => (
                  <li key={skill}>{skill}</li>
                ))}
              </ul>
            </div>
            <div className="rounded-md border bg-background p-2 ring-1 ring-primary/10">
              {/* Small icon */}
            </div>
          </Card>
        ))}
      </div>
    </div>
  );
}

// components/footer.tsx
import React from 'react';

export default function Footer() {
  return (
    <footer className="relative py-10 border-t">
      <div aria-hidden="true" className="pointer-events-none absolute inset-x-0 -top-px h-px bg-gradient-to-r from-transparent via-primary/40 to-transparent" />
      <div className="container mx-auto max-w-6xl px-4 flex flex-col md:flex-row items-center justify-between">
        <p className="text-sm text-muted-foreground mb-4 md:mb-0">© 2023 My Portfolio</p>
        <nav className="flex items-center space-x-4">
          {['Privacy', 'Terms', 'Contact'].map((link) => (
            <Link key={link} href={`/${link.toLowerCase()}`} className="text-sm text-muted-foreground hover:text-foreground underline underline-offset-4 decoration-primary/40">
              {link}
            </Link>
          ))}
        </nav>
      </div>
    </footer>
  );
}

// app/globals.css
@keyframes subtle-appear {
  from { opacity: 0; transform: translateY(4px) scale(0.995); }
  to { opacity: 1; transform: translateY(0) scale(1); }
}
.img-appear {
  animation: subtle-appear .6s ease-out both;
}
.elevate {
  transition: transform .3s ease, box-shadow .3s ease;
}
.elevate:hover {
  transform: translateY(-2px);
  box-shadow: 0 10px 24px -12px rgba(0,0,0,0.25);
}
